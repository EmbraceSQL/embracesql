---
to: "{{configuration.embraceSQLRoot}}/{{module.restPath}}.after.ts"
---

import * as types from "{{relativeToRoot}}/context";

export const after : types.{{module.contextName}}Handler = async (_context) => {
  // handle me!
}