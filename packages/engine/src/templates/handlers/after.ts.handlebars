---
to: "{{module.fullPath}}.after.ts"
---

import * as types from "{{relativeToRoot}}/context";

export const after : types.{{module.contextName}}Handler = async (context) => {
// handle me!
return context;
}