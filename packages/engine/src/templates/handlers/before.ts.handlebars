---
to: "{{module.fullPath}}.before.ts"
---

import * as types from "{{relativeToRoot}}/context";

export const before : types.{{module.contextName}}Handler = async (context) => {
// handle me!
return context;
}