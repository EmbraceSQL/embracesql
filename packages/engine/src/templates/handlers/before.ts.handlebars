---
to: "{{configuration.embraceSQLRoot}}/{{module.restPath}}.before.ts"
---

import * as types from "{{relativeToRoot}}/index";

export const before : types.{{module.contextName}}Handler = async (_context) => {
  // handle me!
}