---
to: "{{configuration.embraceSQLRoot}}/{{module.restPath}}.afterError.ts"
---

import * as types from "{{relativeToRoot}}/context";

export const afterError : types.{{module.contextName}}Handler = async (_context) => {
  // handle me!
}